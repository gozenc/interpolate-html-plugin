const InterpolateHtmlPlugin = require("../dist/index")

console.dir(InterpolateHtmlPlugin.prototype)

// test('Escape regex returns string', () => {
//     expect(InterpolateHtmlPlugin.prototype.escapeRegex("0")).toBe("0");
// });

// test('Escape regex throws TypeError on non-string param', () => {
//     expect(() => InterpolateHtmlPlugin.prototype.escapeRegex(0)).toThrow(TypeError)
// });
